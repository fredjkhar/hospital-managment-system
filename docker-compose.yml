version: '3'

services:
  frontend:
    build:
      context: ./path/to/frontend
    ports:
      - "4200:4200" # Expose port 4200 for Angular

  backend:
    build:
      context: ./path/to/backend
    ports:
      - "8080:8080" # Expose port 8080 for Spring Boot
